import { ArrowLeft, Check, Plus, Sparkles } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useState } from 'react'
const initial = [{ skill: 'Python', type: 'Must Have', weight: 10 }, { skill: 'FastAPI', type: 'Preferred', weight: 8 }, { skill: 'AWS', type: 'Preferred', weight: 6 }]
export default function CreateJob() { const [requirements, setRequirements] = useState(initial); const [saved, setSaved] = useState(false); return <div className="mx-auto max-w-5xl"><Link to="/jobs" className="mb-7 flex items-center gap-2 text-xs font-bold text-muted hover:text-ink"><ArrowLeft size={15} />Back to jobs</Link><div className="mb-8"><p className="eyebrow mb-2">Job builder</p><h1 className="text-3xl font-extrabold tracking-tight">Create a new job</h1><p className="mt-2 text-sm text-muted">Describe the role and let HireLens extract what matters.</p></div><div className="grid gap-6 lg:grid-cols-[1fr_.8fr]"><section className="panel p-6"><div className="space-y-5"><label className="block"><span className="mb-2 block text-xs font-bold">Job title</span><input className="w-full rounded-lg border bg-canvas px-3 py-3 text-sm outline-none focus:border-teal" placeholder="e.g. Senior Backend Engineer" defaultValue="Senior Backend Engineer" /></label><label className="block"><span className="mb-2 block text-xs font-bold">Department</span><select className="w-full rounded-lg border bg-canvas px-3 py-3 text-sm outline-none focus:border-teal"><option>Engineering</option><option>Infrastructure</option><option>Data & AI</option></select></label><label className="block"><span className="mb-2 block text-xs font-bold">Job description</span><textarea className="min-h-48 w-full resize-y rounded-lg border bg-canvas px-3 py-3 text-sm leading-6 outline-none focus:border-teal" defaultValue="We are looking for a Senior Backend Engineer to design reliable APIs and services that power our next generation of products. You will own services from architecture through production and work closely with product and data teams." /></label><button className="btn-primary w-full" onClick={() => setSaved(true)}>{saved ? <><Check size={16} />Job saved</> : <><Sparkles size={16} />Extract requirements</>}</button></div></section><section className="panel p-6"><div className="mb-6 flex items-center justify-between"><div><p className="eyebrow mb-2">AI extraction</p><h2 className="text-xl font-extrabold">Requirements</h2></div><span className="rounded-full bg-mint px-2 py-1 text-[10px] font-bold text-teal">3 found</span></div><div className="space-y-3">{requirements.map((req, index) => <div key={req.skill} className="rounded-xl border bg-canvas p-3"><div className="flex items-center justify-between"><span className="text-sm font-bold">{req.skill}</span><button className="text-muted">×</button></div><div className="mt-3 flex items-center justify-between gap-2"><select value={req.type} onChange={e => setRequirements(old => old.map((item, i) => i === index ? { ...item, type: e.target.value } : item))} className="rounded-md border bg-white px-2 py-1.5 text-[11px] font-bold"><option>Must Have</option><option>Preferred</option></select><label className="flex items-center gap-2 text-[11px] text-muted">Weight <input type="number" value={req.weight} onChange={e => setRequirements(old => old.map((item, i) => i === index ? { ...item, weight: e.target.value } : item))} className="w-12 rounded-md border bg-white px-2 py-1.5 text-center font-bold text-ink" /></label></div></div>)}</div><button className="btn-soft mt-4 w-full"><Plus size={15} />Add requirement</button></section></div></div> }