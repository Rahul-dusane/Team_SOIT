import { ArrowDownRight, ArrowUpRight } from 'lucide-react'
export default function StatCard({ label, value, change, icon: Icon, tone = 'teal' }) { return <div className="panel p-5"><div className="mb-5 flex items-start justify-between"><div className={`grid h-10 w-10 place-items-center rounded-xl ${tone === 'coral' ? 'bg-[#fff0eb] text-coral' : 'bg-mint text-teal'}`}><Icon size={19} /></div>{typeof change === 'number' && Number.isFinite(change) && <span className={`flex items-center gap-0.5 text-xs font-bold ${change > 0 ? 'text-teal' : 'text-coral'}`}>{change > 0 ? <ArrowUpRight size={14} /> : <ArrowDownRight size={14} />}{Math.abs(change)}%</span>}</div><p className="text-sm text-muted">{label}</p><p className="mt-1 text-3xl font-extrabold tracking-tight">{value}</p></div> }