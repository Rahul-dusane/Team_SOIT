import { X } from 'lucide-react'
export default function EvidenceDrawer({ open, onClose, skill = 'FastAPI' }) { return <><div className={`fixed inset-0 z-40 bg-ink/20 transition ${open ? 'visible opacity-100' : 'invisible opacity-0'}`} onClick={onClose} /><aside className={`fixed inset-y-0 right-0 z-50 w-full max-w-md bg-white p-7 shadow-2xl transition-transform ${open ? 'translate-x-0' : 'translate-x-full'}`}><div className="flex items-center justify-between"><div><p className="eyebrow mb-2">Evidence found</p><h2 className="text-2xl font-extrabold">{skill}</h2></div><button onClick={onClose} className="rounded-lg bg-canvas p-2 text-muted hover:text-ink"><X size={18} /></button></div><div className="mt-10 rounded-xl border-l-4 border-teal bg-mint/50 p-5"><p className="mb-3 text-xs font-bold uppercase tracking-wider text-teal">Resume evidence</p><p className="text-sm font-semibold leading-7 text-ink">“Developed production FastAPI microservices using Python, handling 50k+ daily requests with automated testing and observability.”</p><p className="mt-4 text-xs text-muted">Page 2 · Rahul Sharma resume</p></div><div className="mt-6 flex items-center justify-between border-b pb-5"><span className="text-sm text-muted">Confidence</span><span className="font-extrabold text-teal">94%</span></div><p className="mt-6 text-xs leading-6 text-muted">This evidence was extracted by HireLens Resume Agent and semantically matched to the job requirement.</p></aside></> }