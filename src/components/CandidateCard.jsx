import { ArrowUpRight, Briefcase, GraduationCap } from 'lucide-react'
import { Link } from 'react-router-dom'
import MatchScore from './MatchScore'
import SkillBadge from './SkillBadge'
export default function CandidateCard({ candidate }) { return <div className="panel p-5 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-start justify-between"><div className="flex gap-3"><div className={`grid h-11 w-11 place-items-center rounded-full text-xs font-extrabold ${candidate.avatar}`}>{candidate.initials}</div><div><h3 className="font-bold">{candidate.name}</h3><p className="mt-0.5 text-xs text-muted">{candidate.role}</p></div></div><MatchScore score={candidate.score} /></div><div className="my-5 grid grid-cols-2 gap-3 text-xs text-muted"><span className="flex items-center gap-2"><Briefcase size={14} />{candidate.experience} experience</span><span className="flex items-center gap-2"><GraduationCap size={14} />{candidate.education.split(',')[0]}</span></div><div className="flex flex-wrap gap-1.5">{candidate.skills.slice(0, 4).map(skill => <SkillBadge key={skill}>{skill}</SkillBadge>)}</div><Link to={`/matches/${candidate.id}`} className="mt-5 flex items-center justify-between border-t pt-4 text-xs font-bold text-teal">View full profile <ArrowUpRight size={15} /></Link></div> }