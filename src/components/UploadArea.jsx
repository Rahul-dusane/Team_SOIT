import { useState } from 'react'
import { CheckCircle2, FileUp, LoaderCircle, UploadCloud } from 'lucide-react'
export default function UploadArea() { const [files, setFiles] = useState([{ name: 'Rahul.pdf', done: true }, { name: 'Priya.pdf', done: true }, { name: 'Amit.pdf', done: false }]); const addFiles = e => { const added = [...e.target.files].map(file => ({ name: file.name, done: false })); setFiles(old => [...old, ...added]); setTimeout(() => setFiles(old => old.map(file => ({ ...file, done: true }))), 1200) }; return <div className="panel p-5"><label className="flex cursor-pointer flex-col items-center justify-center rounded-xl border-2 border-dashed border-[#cfe0dc] bg-[#fbfdfc] px-5 py-10 text-center transition hover:border-teal"><input type="file" multiple accept=".pdf,.docx" className="hidden" onChange={addFiles} /><span className="mb-3 grid h-12 w-12 place-items-center rounded-full bg-mint text-teal"><UploadCloud size={22} /></span><span className="text-sm font-bold">Drop resumes here or browse files</span><span className="mt-2 text-xs text-muted">PDF and DOCX · Max 10 MB per file</span></label><div className="mt-5 space-y-2">{files.map(file => <div key={file.name} className="flex items-center justify-between rounded-lg bg-canvas px-3 py-3"><div className="flex items-center gap-2 text-xs font-semibold"><FileUp size={15} className="text-muted" />{file.name}</div>{file.done ? <span className="flex items-center gap-1 text-[11px] font-bold text-teal"><CheckCircle2 size={15} />Parsed</span> : <span className="flex items-center gap-1 text-[11px] font-bold text-[#a17612]"><LoaderCircle className="animate-spin" size={14} />Processing...</span>}</div>)}</div></div> }